# 日志与界面文案审核

## 桌面端修改计划

- 范围：`free_rollcall_app.py` 的日志、状态提示和相关布局；保留业务层查询、登录及线程调用方式
- 文案：沿用已确认的命令行用语，使用“获取”，不加中文句号，保留加载提示的省略号
- 状态：分别展示登录异常、凭证缺失、课程获取异常、空课程列表、签到查询异常、无签到记录和无数字签到码，避免直接展示空值
- 布局：课程图标和箭头固定列，课程名称与标识左对齐，长名称换行；结果卡片和多行提示居中
- 验证：检查语法与差异，以模拟数据覆盖上述状态，并检查长短课程名称和结果页面的布局
- 本轮不调整业务层接口，不新增依赖，不修改旧目录、打包产物或用户其他未提交内容，不创建备份

## 验证记录

- 语法、模块导入和差异格式检查通过
- 使用内存模拟数据，在实际桌面窗口中检查了 6 种登录或课程获取失败情况，以及成功登录后的课程展示
- 覆盖数字签到码、无签到记录、查询异常、无数字签到码、未知状态等 5 种结果；异常页面不会再显示“暂无签到记录”
- 检查课程名称、标识、箭头的实际控件坐标一致，长名称能够换行，名称和标识均绑定课程点击操作
- 目视检查课程列表和数字签到码结果页，确认课程列对齐、长名称完整展示、结果信息居中；查询日志的字段标签等宽对齐
- 验证只使用模拟数据，未进行真实登录或在线查询；没有新增测试脚本、依赖或备份
